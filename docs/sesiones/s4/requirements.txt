# Dependencias del kernel interactivo de la sesión 4 (Spark en modo local).
# La serie de PySpark aceptada por el curso es >4,<4.2.
# El envío a YARN mediante spark-submit usa un entorno virtual propio (ver
# la sección "Driver, ejecutores y YARN" del notebook), no este fichero.
pyspark>4,<4.2
